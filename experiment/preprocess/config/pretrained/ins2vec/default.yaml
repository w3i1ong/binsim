dataset:
  # the directory of the corpus.
  corpus: /home/anonymous/Documents/binsim-corpus/processed/tiny-corpus
  # Which corpus should our language model be trained on?
  name: test
  # What kind of generated text should we train on?
  label: test
  # What kind of Graph will we use?
  type: InsCFG

model:
  name: 'ins2vec'
  # should we train a separate model for each architecture?
  train-per-arch: True
  # model-specific kwargs
  model_kwargs:
    embed_dim: 100
